#include "common.h"
#include "hdr10.h"

//////////////////////////////////////////////////////////////////////////////////////////
// Pixel
float4 main ( p_TL I ) : SV_Target
{
	if (HDR_IS_ENABLED) {
		return s_base.Sample( smp_base, I.Tex0 )*I.Color*0.7f;
	} else {
		return s_base.Sample( smp_base, I.Tex0 )*I.Color;
	}
	

//	float4 res = s_base.Sample( smp_base, I.Tex0 )*I.Color;
//	clip(res-m_AlphaRef);
//	clip(res-0.5);
//	return res;
}
