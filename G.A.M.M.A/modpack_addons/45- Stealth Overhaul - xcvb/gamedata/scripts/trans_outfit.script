-- moved to visual_memory_manager
-- bind_stalker.script is still calling for this function
-- don't want to add bind_stalker too, so this is easier ;]
-- script is going to be removed in the next Anomaly update anyway (hopefully)

function timer_trans() 
end