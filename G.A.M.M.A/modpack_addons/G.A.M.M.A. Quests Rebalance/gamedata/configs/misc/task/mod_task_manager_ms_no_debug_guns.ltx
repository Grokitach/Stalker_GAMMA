;; Removes debug-only guns from Mortal Sin completion rewards.

; Replaced wpn_pkm_zulus reward with wpn_pkm_zenit_siber
![lttz_ms_zone_hero_dolg]
on_complete = %=reward_item(wpn_pkm_zenit_siber) =reward_money(25000) =complete_task_inc_goodwill(100:dolg) =remove_special_task_squad(lim_dolg_assault_squad)%

; wpn_svd_nimble replaced with wpn_svds_pmc (there are no SVDs in GAMMA that are not debug)
![lttz_ms_zone_hero_killer]
on_complete = %=reward_item(wpn_svds_pmc) =reward_money(25000) =complete_task_inc_goodwill(100:killer) =remove_special_task_squad(lim_killer_assault_squad)%
